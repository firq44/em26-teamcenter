## Settings file was created by plugin EM LlmService v0.1.0
## Plugin GUID: com.esportsmanager.modkit.llmservice

[Llm]

## LLM 服务总开关。默认 false：关闭时不构造客户端、不注册服务、不发任何网络请求。本服务不内置任何 API key，开启前请在下面的 Preset.* 段填写你自己的端点与 key，或指向本地 OpenAI 兼容端点。
# Setting type: Boolean
# Default value: false
Enabled = false

## 默认预设名。LlmRequest 不指定预设名时用它。须出现在Presets 清单里。
# Setting type: String
# Default value: default
DefaultPreset = default

## 预设名清单，逗号分隔。每个名字对应一个 Preset.<名字> 配置段。不同消费者 Mod 可各指各的预设，互不干扰。
# Setting type: String
# Default value: default
Presets = default

